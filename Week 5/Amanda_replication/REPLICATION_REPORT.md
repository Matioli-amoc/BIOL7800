# Amanda's independent replication

**Verdict: byte-identical (after documented path repairs).** The final complete independent run passed the repository's unmodified `bash verify.sh`: all **13/13** canonical SHA-256 checks matched (three public input files and ten scientific output files). See `evidence/verification.log` (exit 0) and `evidence/analysis_attempt_5.log` (exit 0). All seven stages completed successfully, and all 12 diagnostic plots were generated. The first complete run also passed verification.

Repository: https://github.com/SachidaPokhrel/Replicability.git  
Commit: `b2f10ff04b38e45c0362f39a13bd33a6461da505`  
Run date: September 22, 2026 America/Chicago (September 23 UTC).

The agent cloned the repository, inspected its documentation/scripts/environment and archived committed outputs before analysis. It used `setup_and_run.sh` to install the declared Conda environment and download AP024628.1 and the first 300,000 paired SRA spots of DRR172337. Both FASTQs and the reference matched canonical hashes. The analysis used BWA, SAMtools, NextPolish task 1212 and Merqury k=21 with four threads. Main tool versions matched the canonical provenance; exact resolved dependencies are in `evidence/environment.explicit.txt` and `environment.resolved.yml`. Canonical outputs were never used as analysis inputs.

## Scientific comparison

Each value below is identical between the canonical and reproduced run.

| Metric | Original assembly | Polished assembly |
|---|---:|---:|
| Contigs | 1 | 1 |
| Total bp / N50 bp | 4,217,110 | 4,217,111 |
| Primary mapped % | 99.52 | 99.52 |
| Properly paired % | 99.17 | 99.17 |
| Mean depth | 39.254981 | 39.254982 |
| Breadth at ≥1× % | 99.922791 | 99.922791 |
| Merqury QV | 51.4374 | 51.4374 |
| Merqury estimated error rate | 7.18218e-06 | 7.18218e-06 |
| Merqury completeness % | 99.8997 | 99.9001 |

Polishing adds one C base after a common prefix of 1,333,783 bases (an equivalent placement within a repeat is possible). Case-normalized sequence comparison confirms the remaining bases are identical. Completeness increases by 0.0004 percentage points; QV and mapping percentages remain unchanged at reported precision. Thus the canonical conclusion holds: a very small sequence change and completeness increase, with essentially unchanged quality metrics. These metrics reuse the polishing reads for evaluation and do not establish an independent improvement in biological accuracy.

## Problems and interventions

- Sandbox DNS restrictions initially blocked cloning and Conda downloads; approved network retries succeeded. No manual analysis commands were required from the user.
- NextPolish did not handle spaces in `Week 5`: both generated job paths and its installed launcher contained unquoted paths. A `/tmp/amanda_replication_20260922` symlink points to the requested repository directory; data remain there. `run.replication.sh` changes only ROOT, and `evidence/bin/nextPolish` changes only the embedded installation path. Original repository scripts remain unmodified. Exact patches and failed-job logs are preserved.
- Conda's R shell wrapper also contained unquoted paths, so Merqury silently failed to draw optional diagnostic plots while successfully calculating required metrics. The installed R wrapper's prefix was replaced with the same alias; its original and patch are preserved. R/ggplot2 then loaded successfully.
- An agent-side edit to a running Bash wrapper accidentally restarted analysis and truncated its combined log after the first complete run. The duplicate was stopped; completed outputs, remaining stage logs, successful NextPolish log and canonical verification were preserved. A final full run with a stable wrapper completed successfully and left complete evidence. This did not alter the canonical manifest or substitute canonical results.

## Submission and evidence

`CHECKSUMS.txt` at the repository root is the classmate's unchanged canonical reference. `CHECKSUMS.generated.txt` is generated by this replication. `submission/CHECKSUMS.txt` contains a copy of the generated manifest; its paths are relative to the repository root. The pre-existing checksum file in the parent Week 5 directory was untouched.

`evidence/AGENT_ACTIONS.md` is an agent-authored execution record, not a verbatim chat export. Raw setup/analysis logs, exit statuses, stage logs, failure diagnostics, environment specifications, comparison scripts and patches provide terminal evidence. The course can additionally use an export of this conversation if a literal agent transcript is required.

## Agent interaction
The replication required 1 initial user prompt to the coding agent. No additional user prompts or manual analysis commands were required.
